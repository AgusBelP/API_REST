const models = {
    usersModel : require('../models/nosql/users.model'),
    tracksModel : require('../models/nosql/tracks.model'),
    storageModel : require('../models/nosql/storage.model')
};

module.exports = models